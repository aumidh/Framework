package analyser;
/**
 * The enum values for each possible construct in the XML workflow file.
 * @author Sardar Hussain
 *
 */
public enum WFConstructsType {
SERVICE,AND_SPLIT,AND_JOIN,OR_SPLIT,OR_JOIN,XOR_SPLIT,XOR_JOIN,SEQUENCE
}
